import React, {createContext} from 'react'

export const InterviewNexusContext = createContext(null)

const InterviewNexusContextProvider = ({props}) => {
    const contextValue = {}
}

export default InterviewNexusContextProvider
